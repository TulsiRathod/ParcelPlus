# @reach/combobox (a leftover example dep of use-places-autocomplete) pins React
# 16/17, which conflicts with this project's React 18 under npm's strict peer
# resolution. Use legacy peer resolution so installs (local and CI) succeed.
legacy-peer-deps=true
